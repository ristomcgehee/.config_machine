---
  - name: "Install packages for all"
    hosts: all

    tasks:
      - apt:
          name:
            - gcc
            - g++
            - jq
            - make
            - tree

      - snap:
          name: code
          classic: yes

  - name: "Install packages for personal_work"
    hosts: personal_work

    tasks:
      - snap:
          name:
            - spotify

